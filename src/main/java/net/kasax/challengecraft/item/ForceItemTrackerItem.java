package net.kasax.challengecraft.item;

import net.minecraft.world.item.Item;

/** Opens the Force Item Battle game screen when used (see Chal_45's UseItemCallback). */
public class ForceItemTrackerItem extends Item {
    public ForceItemTrackerItem(Properties settings) {
        super(settings);
    }
}
